diff --git a/programming_examples/getting_started/01_SAXPY/saxpy.cc b/programming_examples/getting_started/01_SAXPY/saxpy.cc
index b83c60f8a6e..c9f2700a038 100644
--- a/programming_examples/getting_started/01_SAXPY/saxpy.cc
+++ b/programming_examples/getting_started/01_SAXPY/saxpy.cc
@@ -14,24 +14,27 @@
 
 #include <aie_api/aie.hpp>
 
+constexpr int vec_size = 4096;
+constexpr int lanes = 64;
+
 // NOTE: Both kernels below are hardcoded for N=4096 elements. The Python
 // design file (saxpy.py) must be called with a tensor of exactly this size.
 // Calling with any other size will produce silently incorrect results.
 extern "C" {
 void saxpy(bfloat16 *restrict x, bfloat16 *restrict y, bfloat16 *restrict z) {
   event0();
-  ::aie::vector<bfloat16, 64> a_v = ::aie::broadcast<bfloat16, 64>(3.f);
+  ::aie::vector<bfloat16, lanes> a_v = ::aie::broadcast<bfloat16, lanes>(3.f);
 #pragma clang loop min_iteration_count(4)
-  for (int i = 0; i < 4096; i += 64) {
-    ::aie::vector<bfloat16, 64> x_v = ::aie::load_v<64>(x);
-    x += 64;
-    ::aie::vector<bfloat16, 64> y_v = ::aie::load_v<64>(y);
-    y += 64;
-    ::aie::accum<accfloat, 64> ax_v = ::aie::mul(x_v, a_v);
-    ::aie::accum<accfloat, 64> z_v = ::aie::add(ax_v, y_v);
-    ::aie::vector<bfloat16, 64> z_v_converted = z_v.to_vector<bfloat16>();
+  for (int i = 0; i < vec_size; i += lanes) {
+    ::aie::vector<bfloat16, lanes> x_v = ::aie::load_v<lanes>(x);
+    x += lanes;
+    ::aie::vector<bfloat16, lanes> y_v = ::aie::load_v<lanes>(y);
+    y += lanes;
+    ::aie::accum<accfloat, lanes> ax_v = ::aie::mul(x_v, a_v);
+    ::aie::accum<accfloat, lanes> z_v = ::aie::add(ax_v, y_v);
+    ::aie::vector<bfloat16, lanes> z_v_converted = z_v.to_vector<bfloat16>();
     ::aie::store_v(z, z_v_converted);
-    z += 64;
+    z += lanes;
   }
   event1();
 }
diff --git a/programming_examples/getting_started/01_SAXPY/saxpy.py b/programming_examples/getting_started/01_SAXPY/saxpy.py
index 50c44b136bb..79f66cde6a5 100755
--- a/programming_examples/getting_started/01_SAXPY/saxpy.py
+++ b/programming_examples/getting_started/01_SAXPY/saxpy.py
@@ -12,7 +12,8 @@ from aie.iron import CompileTime, ExternalFunction, In, Out
 from aie.iron import ObjectFifo, Program, Runtime, Worker
 from aie.utils.config import cxx_header_path
 from aie.utils.verify import assert_pass
-
+from aie.utils.trace import TraceConfig
+import shutil
 
 # JIT decorator for IRON
 # Decorator to compile an IRON kernel into a binary to run on the NPU.
@@ -25,7 +26,8 @@ def saxpy(
     output: Out,
     *,
     N: CompileTime[int],
-    element_type: CompileTime[type]
+    element_type: CompileTime[type],
+    trace_config: CompileTime[TraceConfig | None] = None
 ):
 
     # --------------------------------------------------------------------------
@@ -63,7 +65,7 @@ def saxpy(
         of_z.release(1)
 
     worker = Worker(
-        core_body, fn_args=[of_x.cons(), of_y.cons(), of_z.prod(), saxpy_kernel]
+        core_body, fn_args=[of_x.cons(), of_y.cons(), of_z.prod(), saxpy_kernel],trace=1
     )
 
     # --------------------------------------------------------------------------
@@ -72,11 +74,13 @@ def saxpy(
 
     rt = Runtime()
     with rt.sequence(in_ty, in_ty, out_ty) as (a_x, a_y, c_z):
+        rt.enable_trace(trace_config.trace_size, [worker])
         rt.start(worker)
         rt.fill(of_x.prod(), a_x)
         rt.fill(of_y.prod(), a_y)
         rt.drain(of_z.cons(), c_z, wait=True)
 
+
     # --------------------------------------------------------------------------
     # Place and generate MLIR program
     # --------------------------------------------------------------------------
@@ -97,15 +101,16 @@ def main():
     input0 = iron.arange(data_size, dtype=element_type, device="npu")
     input1 = iron.arange(data_size, dtype=element_type, device="npu")
     output = iron.zeros_like(input0)
-
+    trace_cfg = TraceConfig(trace_size=8192)
     # JIT-compile the kernel then launches the kernel with the given arguments. Future calls
     # to the kernel will use the same compiled kernel and loaded code objects
-    saxpy(input0, input1, output, N=data_size, element_type=element_type)
+    saxpy(input0, input1, output, N=data_size, element_type=element_type, trace_config=trace_cfg)
 
     # Numpy reference of `3*x + y` and a one-line tolerance check.
     ref = 3 * input0.numpy() + input1.numpy()
     assert_pass(output.numpy(), ref, fail_msg="saxpy output does not match 3*x + y")
-
+    # trace_cfg.trace_to_json(cfg.physical_mlir_path, str(out / "trace.json"))
+    shutil.copy(trace_cfg.physical_mlir_path, "input_with_addresses.mlir")
 
 if __name__ == "__main__":
     main()
diff --git a/programming_examples/getting_started/03_matrix_multiplication_single_core/matrix_multiplication_single_core.py b/programming_examples/getting_started/03_matrix_multiplication_single_core/matrix_multiplication_single_core.py
index 29883c4c3ee..4c0cde1de46 100755
--- a/programming_examples/getting_started/03_matrix_multiplication_single_core/matrix_multiplication_single_core.py
+++ b/programming_examples/getting_started/03_matrix_multiplication_single_core/matrix_multiplication_single_core.py
@@ -33,9 +33,13 @@ from aie.iron import (
 from aie.iron.controlflow import range_
 from aie.helpers.taplib import TensorAccessPattern, TensorTiler2D
 from aie.utils.verify import assert_pass
+from aie.utils.trace import TraceConfig
+import shutil
 
 # Tile size moved to/from the compute cores via mem tiles.
-_TILE_M = _TILE_K = _TILE_N = 64
+_TILE_M = 32
+_TILE_K = 64
+_TILE_N = 64
 # AIE kernel intrinsic (r, s, t) used to drive the DMA layout transforms
 # below (r*s / s*t / r*t sub-tiles) is read from `matmul_kernel.mac_dims`
 # inside the design — see aie_kernels/aie2{,p}/mm.cc for the per-arch
@@ -53,6 +57,7 @@ def matrix_multiplication_single_core(
     K: CompileTime[int],
     N: CompileTime[int],
     element_type: CompileTime[type],
+    trace_config: CompileTime[TraceConfig],   # ny
 ):
     m, k, n = _TILE_M, _TILE_K, _TILE_N
 
@@ -137,6 +142,7 @@ def matrix_multiplication_single_core(
 
     rt = Runtime()
     with rt.sequence(A_ty, B_ty, C_ty) as (A, B, C):
+        rt.enable_trace(trace_config.trace_size,[worker])
         rt.start(worker)
         for tile_row in range(M // m):
             task_group = rt.task_group()
@@ -154,10 +160,10 @@ def matrix_multiplication_single_core(
     return Program(iron.get_current_device(), rt).resolve_program()
 
 
-def aot_compile(M: int, K: int, N: int, element_type) -> None:
+def aot_compile(M: int, K: int, N: int, element_type,trace_config:TraceConfig) -> None:
     """Pre-compile the JIT design for one shape and print the artifact paths."""
     xclbin, insts = matrix_multiplication_single_core.specialize(
-        M=M, K=K, N=N, element_type=element_type
+        M=M, K=K, N=N, element_type=element_type, trace_config=trace_config
     ).compile()
     print(f"  AOT compiled matmul {M}x{K}x{N} {np.dtype(element_type).name}")
     print(f"    xclbin: {xclbin}")
@@ -192,6 +198,7 @@ def main():
         dict(M=256, K=256, N=256, element_type=np.int16),
         dict(M=512, K=512, N=512, element_type=np.int16),
     ]
+    trace_cfg = TraceConfig(trace_size=8192)
 
     # AOT phase: pre-compile both shapes upfront so the artifacts exist on
     # disk before any kernel runs.  This is purely opt-in — the @iron.jit
@@ -199,12 +206,26 @@ def main():
     # compile cost on first invocation instead.
     print("=== AOT pre-compile phase ===")
     for kw in shapes:
-        aot_compile(**kw)
-
-    # Runtime dispatch.  Each call uses the same JIT-decorated function and
-    # finds its compiled artifact already in the on-disk cache.
-    for kw in shapes:
-        run_and_verify(**kw)
+        M, K, N, et = kw["M"], kw["K"], kw["N"], kw["element_type"]
+        tag = f"{M}x{K}x{N}"
+        print(f"\n=== matmul {tag} ===")
+
+        cfg = TraceConfig(trace_size=524288, trace_file=f"trace_{tag}.txt")
+
+        input0 = iron.randint(0, 256, (M, K), dtype=et, device="npu")
+        input1 = iron.randint(0, 256, (K, N), dtype=et, device="npu")
+        output = iron.zeros(M * N, dtype=et, device="npu")
+        ref = np.matmul(input0.numpy(), input1.numpy())
+
+        matrix_multiplication_single_core(
+            input0, input1, output, **kw, trace_config=cfg
+        )
+
+        assert_pass(output.numpy(), ref.flatten(),
+                    fail_msg=f"matmul {tag} mismatch", print_pass=False)
+        print("  PASS")
+        shutil.copy(cfg.physical_mlir_path, "input_with_addresses.mlir")
+        # cfg.trace_to_json(cfg.physical_mlir_path, f"trace_{tag}.json")
 
     print("\nAll variants PASS!")
 
diff --git a/programming_examples/getting_started/03_matrix_multiplication_single_core/matrix_multiplication_single_core2.py b/programming_examples/getting_started/03_matrix_multiplication_single_core/matrix_multiplication_single_core2.py
index afa6b6d115d..b0943d63e9a 100644
--- a/programming_examples/getting_started/03_matrix_multiplication_single_core/matrix_multiplication_single_core2.py
+++ b/programming_examples/getting_started/03_matrix_multiplication_single_core/matrix_multiplication_single_core2.py
@@ -37,8 +37,8 @@ from aie.utils.trace import TraceConfig
 import shutil
 
 # Tile size moved to/from the compute cores via mem tiles.
-_TILE_M = 8
-_TILE_K = 64
+_TILE_M = 32
+_TILE_K = 32
 _TILE_N = 128
 # AIE kernel intrinsic (r, s, t) used to drive the DMA layout transforms
 # below (r*s / s*t / r*t sub-tiles) is read from `matmul_kernel.mac_dims`
@@ -116,12 +116,13 @@ def matrix_multiplication_single_core(
             elem_out = of_c.acquire(1)
             for i in range_(m * n):
                 elem_out[i] = 0
-            for _ in range_(K // k):
-                elem_in_a = of_a.acquire(1)
-                elem_in_b = of_b.acquire(1)
-                matmul(elem_in_a, elem_in_b, elem_out)
-                of_a.release(1)
-                of_b.release(1)
+            for _ in range_(K // k // 2):
+                elem_in_a = of_a.acquire(2)
+                elem_in_b = of_b.acquire(2)
+                matmul(elem_in_a[0], elem_in_b[0], elem_out)
+                matmul(elem_in_a[1], elem_in_b[1], elem_out)
+                of_a.release(2)
+                of_b.release(2)
             of_c.release(1)
 
     worker = Worker(
